<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.iparking">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IParking">
        <activity
            android:name=".activities.StayActivity"
            android:exported="true" />
        <activity
            android:name=".activities.BeginStayActivity"
            android:exported="true" />
        <activity
            android:name=".activities.RegisterVehicleActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PayActivity"
            android:exported="true" />
        <activity
            android:name=".activities.BookSpotActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SigninActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PermissionsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MapActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>